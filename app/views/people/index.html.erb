<% @page_title = title("Meet the Team") %>

<%= render :partial => 'index_header' %>

<% for group in [ @scientists, @staff, @grads, @studs ] %>
	<%= render :partial => 'group_of_people', :locals => { :group => group } %>
<% end %>

<%= render :partial => 'group_of_people', :locals => { :group => @alumni, :brief => true } %>

<%= render :partial => 'new_person_link' %>