<% content_for :head do %>
	<%= stylesheet_link_tag 'events' %>
<% end %>


<% @page_title = title("Edit event - " + @event.title) %>

<h1>Editing event</h1>

<%= render :partial => 'new_edit', :locals => { :action => 'Update' } %>

<%= link_to 'Show', @event %> |
<%= link_to 'Back', events_path %>