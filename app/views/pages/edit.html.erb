<% content_for :head do %>
	<%= stylesheet_link_tag 'pages' %>
<% end %>

<% @page_title = title("Edit a resource page") %>

<h1>Editing page</h1>

<%= render :partial => 'new_edit', :locals => { :action => 'Update' } %>

<%= link_to 'Show', @page %> |
<%= link_to 'Back', pages_path %>