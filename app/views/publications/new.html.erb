<% content_for :head do %>
	<%= stylesheet_link_tag 'publications' %>
<% end %>

<% @page_title = title("Create a new publication") %>

<h1>New publication</h1>

<%= render :partial => 'new_edit', :locals => { :action => 'Create' } %>

<%= link_to 'Back', publications_path %>